# SQL Server / PCP DB
# Pode usar variáveis genéricas (DB_*) ou específicas do PCP (PCP_DB_*).
# Ajuste o driver conforme o ambiente (ex.: ODBC Driver 17/18 for SQL Server).
DB_DRIVER=ODBC Driver 17 for SQL Server
DB_SERVER=192.168.163.20
DB_NAME=PROTHEUS11
DB_USER=sa
DB_PASS=Flamb@2014

# Alternativa específica para PCP (mantida para compatibilidade entre branches)
PCP_DB_SERVER=sqlserver-host,1433
PCP_DB_UID=sa
PCP_DB_PWD=YourStrong!Passw0rd
PCP_DB_NAME=PCP_DB

# Optional if Protheus is on another DB name in same server
PROTHEUS_DB_NAME=PROTHEUS_DB

# Legacy app variables
FILIAL=01
EMPRESA=010

# Raw conn string fallback for Protheus SQL adapter if needed by future tasks
PROTHEUS_CONN_STR=

# Async infra
REDIS_URL=redis://redis:6379/0
